{
  "name": "ecocycle-monorepo",
  "version": "1.0.0",
  "description": "Smart Plastic Waste Collection & Recycling Portal",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend && npm install --prefix vanilla-frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:vanilla": "npm run dev --prefix vanilla-frontend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:all-vanilla": "concurrently \"npm run dev:backend\" \"npm run dev:vanilla\"",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "build": "npm run build:backend && npm run build:frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
