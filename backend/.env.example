PORT=5000
# Leave MONGO_URI blank to automatically use the in-memory MongoDB server for testing
MONGO_URI=
JWT_SECRET=supersecretkey_ecocycle_2026

# Optional: Cloudinary Configuration (falls back to local uploads if empty)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Optional: Mailer Configuration (falls back to console logs if empty)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@ecocycle.com
